document.addEventListener("DOMContentLoaded", function () {
  console.log("DOM fully loaded and parsed");

  // Project links functionality can be added here if needed
  // Currently, links work natively with target="_blank"
});
